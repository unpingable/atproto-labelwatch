schema = "project.observation-binding/v1"
producer = "labelwatch.ops-status"
output_schema = "project.ops.status/v1"
kind = "exec/v1"
argv = ["python3", "-m", "labelwatch.ops_status", "--format", "json"]
environment = { PYTHONPATH = "src" }
