# Roadmap

- Add coordination/co-movement rules (synchronized spikes, shared-target overlap).
- Optional meta-labeler emission mode.
- Prometheus metrics endpoint.
- Simple inspection web port (`/health`, `/recent-alerts`).
- Labeler policy/document checking (weak provenance signals).
